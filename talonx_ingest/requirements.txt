python-dotenv>=1.0
aiohttp>=3.9
langchain-text-splitters>=0.2
beautifulsoup4>=4.12
lxml>=5.0
chromadb>=0.5
sentence-transformers>=2.7
websockets>=12.0
yfinance>=0.2.40
pydantic>=2.0
redis>=5.0
feedparser>=6.0
# Event-intelligence session bucketing (talonx_ingest.intelligence.sessions):
# NYSE trading-day / holiday / half-day calendar. The module degrades to a
# weekday-only fallback (flagged) if this is absent, but the live product
# needs it for correct BMO/RTH/AMC/NON_TRADING_DAY classification.
exchange-calendars>=4.5
