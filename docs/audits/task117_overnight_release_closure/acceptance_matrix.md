# Task 117 — Overnight Release Closure — Acceptance Matrix

Start SHA `b485e3f86508ae5e75aa1ed8a9cd71410c76e7e7` · branch
`research/talonx-strategy-validation` · V2 fingerprint `11107198c5b81237`
(unchanged) · production `ingestion_ledger.db` md5 `2ae2105c6f51a4af4b3b80dc06e5f30f`
(unchanged) · no live session, no external send.

All tests run with `./.venv/Scripts/python.exe -m pytest`.

| # | requirement | result | evidence |
|---|---|---|---|
| **1A** | delivery mode validated BEFORE expiry / DB mutation / network; only explicit `enabled` sends; invalid modes fail closed | **PASS** | `pipeline.process_pending` raises `InvalidDeliveryMode` at the top (line ~337) before `recover_in_flight` / `expire_stale` / sender; `test_task117_delivery_reliability.py::test_invalid_mode_fails_closed_before_any_mutation` |
| **1B** | durable attempt identity + IN_FLIGHT state persisted (committed) before the network; restart / competing drainer → AMBIGUOUS, never blind-retried; no network-level exactly-once claim | **PASS** | `outbox.claim_for_send` (PENDING→IN_FLIGHT commit), `recover_in_flight(stale_after=90s)`→AMBIGUOUS, `release_claim`; adapter `send(retry_ambiguous=False)`; `test_claim_is_persisted_before_the_network`, `test_competing_drainer_cannot_send_a_claimed_row`, `test_restart_recovers_a_stale_in_flight_row_to_ambiguous`, `test_lower_layer_does_not_blind_retry_an_ambiguous_timeout` |
| **1C** | disabled holds PENDING (sendable after enablement); simulate never SENT; SENT records ack incl. Telegram message id; audit log write documented separately; permanent vs transient distinguishable | **PASS** | `mark_sent(message_id=…)`, `transport_message_id`; `test_sent_records_message_id_disabled_holds_simulate_no_sent`, `test_permanent_vs_transient_stay_distinguishable` |
| **1D** | expiry / retry-eligibility / send timestamps evaluated at the actual decision/send time | **PASS** | `process_pending` recomputes `now = datetime.now(utc)` when not supplied; per-row `send_now`; `test_decision_uses_the_actual_send_time` |
| **1E** | delivery work bounded without cancellation holes; unexpected errors visible, never silently terminate polling or fold into healthy | **PASS** | `runner.deliver_cycle` wraps each route in `asyncio.wait_for`; `CancelledError` re-raised; `Exception`→`logger.error` + `summary["ok"]=False`; `DrainResult.errors`; `test_unexpected_sender_error_is_visible_not_swallowed`, `test_cancellation_mid_send_marks_ambiguous_and_propagates` |
| **2 digest** | IMMEDIATE-then-DIGEST-loop is not evidence — real aggregation + schedule + restart dedup; official routing authority; freshness by trustworthy timestamps; old filings enqueued today are not fresh; unestablished freshness → hold; auditable expiry (no deletion, no EXPIRED→SENT); recompute counts at the rehearsal cutoff; CRITICAL subset reviewed without send; first enabled cycle cannot flood | **PASS** | `process_digest` aggregates DIGEST rows into ONE message, `last_digest_bucket` restart-safe dedup; `test_task117_digest_semantics.py` (5); backlog rehearsal `backlog_rehearsal.md` (9,843 → 9,841 EXPIRED / 2 PENDING @ 2026-09-11T13:30Z; 9,843 EXPIRED @ 2026-09-12); `expire_stale` uses `min(event_time, enqueue_time)`; production untouched |
| **3 atomic start** | atomic single-instance guard over check→spawn→registration; 2 concurrent launches → 1 owner; `--force` cannot bypass a live writer; stale ownership recovered only after verifying prior owner gone; partial-startup cleanup; unrelated procs & Redis untouched; restart after clean shutdown OK; truthful NOT_STARTED/STARTING/READY/FAILED_WITH_RESIDUALS; READY includes dashboard + first-tick | **PASS** | `talonx_ops/prospective/lock.py` `os.open(O_CREAT\|O_EXCL)`; `proc.start_stack` acquires before the process scan, releases on `BaseException`; `startup_verdict` MANDATORY = supervisor+companion+**dashboard_8787**+heartbeat_fresh; `test_task117_single_writer_lock.py` incl. **real 2-subprocess** `test_two_real_concurrent_starts_yield_exactly_one_owner`; `test_task117_startup_verdict.py` (16); `startup_concurrency_acceptance.md` |
| **4 dashboard** | actual :8787 SPA rendered vs isolated state; delivery states (disabled/pending/sending-ambiguous/sent/failed/expired); last send + acks across official domains; source/processing/pricing/coverage/delivery health separate; scoped V2 funnel; current-session EOD; startup states; positions visible during source/delivery failure; Trading/Intelligence/Experimental distinct | **PASS** | 7 `ISOLATED_FIXTURE_*.png` from `dashboard_web.py` on `TALONX_HOME` override; new `card_delivery` block (Intelligence) + `official_telegram_last_send` card (Paper/EOD) in `dashboard_read.py` **and** the SPA frontend `dashboard_web_static/index.html`; `dashboard_acceptance.md`; `test_task117_lane_accounting.py`, dashboard regression 445 pass |
| **4 accounting** | preserve final-day counters + acquisition timestamps at EOD; separate lanes + unique setups; include pending/in-flight; smallest correction so shared counters are not shown as official publications; historical evidence preserved; final-day 158=154+4 does NOT reconstruct the 16:24 snapshot; 4 off-counter dispositions verified from records, not defined as remainder | **PASS** | `lane_accounting.py`: `off_counter_dispositions_from_records` (all 0 — no throttle/cooldown/reval records exist), `final_day_funnel_closure.residual_attribution = UNEXPLAINED_FROM_RECORDS`, `historical_16_24_ping_snapshot = NOT_RECONSTRUCTABLE`, `historical_94_candidate_gap = SUPERSEDED_BY_FINAL_DAY_RECONCILIATION`; dashboard `published_proxy_alerts_today = dispatch_audit.alerts` (0), comingled Redis tagged `lane_attributable:false`; `accounting_corrections.md` |
| **5 timestamps** | genuine Z/+00:00 kept as UTC; naive only under explicit source contract; SGML-Eastern kept separate from submissions JSON; no production-history shift (last manifest 0 VERIFIED_CORRECTION); callers reviewed for double-conversion / missing provenance / causal-cutoff effects; S1–S3 anomaly stays visible; live-source boundary tested | **PASS (carried from Task E + re-verified)** | `edgar_normalize.parse_acceptance_datetime_ex`: aware/explicit-offset → `astimezone(utc)` (single conversion), naive+submissions → UTC+`ACCEPTANCE_OFFSET_ABSENT`, naive+`sgml_header` → Eastern DST-aware (separate, not wired); caller scan (`sessions.to_et`, `insider/store._iso/_dt`) all guard `tzinfo is None` — no double conversion; Task E `docs/audits/task117_delivery_timestamp_completion/` manifest = 0 VERIFIED_CORRECTION; S1–S3 ~4h persisted-vs-retrieved anomaly still `UNRESOLVED` (`timestamp_source_contract.md`); timestamp/edgar/insider regression 194 pass |
| **6 rehearsal** | one bounded end-to-end: source→enrichment→queue→routing→intercepted ack→dashboard→restart→EOD→shutdown; disabled→enabled same event; fresh + stale backlog; digest schedule; transient failure; timeout/cancel after possible acceptance; restart w/ in-flight; two competing starts; source failure w/ open V2 position; no dup delivery / false SENT / dup writer / lost pending; ownership-safe shutdown; no full-day disabled live session | **PASS** | `test_task117_release_rehearsal.py::test_bounded_release_rehearsal` → `results/task117_overnight_release_closure_evidence/release_rehearsal.json` (`overall_PASS: true`); V2 side + open-position-source-failure in `test_task117_overnight_e2e.py` (3); `test_task117_deployment_rehearsal.py` |
| **7 profitability handoff** | isolated research worktree + concise contract; no merge to release branch; no strategy change; 39-name V2 baseline / Original selectivity / Experimental separate; datasets + prior-rejection inventory; window/membership/costs/metrics/holdout; promotion criteria before tuning; retrospective watchlist = diagnostic; no broad optimisation tonight | **PASS** | `profitability_research_contract.md`; worktree `C:\workspace\TalonX-task118-profitability` (branch `research/talonx-profitability-2026-09`) created from the release SHA; no run executed |
| **8 package** | 11 files under `docs/audits/task117_overnight_release_closure/`; deployment candidate w/ real commands/flags/config/backup/migration/gates/rollback; EOD ownership described (operator-driven `prospective close`, no new recurring job); coherent commit + normal push; production ledger/source/Redis unchanged; no external messages; no stray processes; clean tree | **PASS** | this directory; `deployment_candidate.md`, `rollback.md`, `MORNING_HANDOFF.md`, `remaining_blockers.md`; commit + push recorded in `MORNING_HANDOFF.md` |

## Not claimed
- Network-level exactly-once delivery. The design gives **durable, single-claim,
  restart-safe** delivery with **visible AMBIGUOUS** on unconfirmable outcomes.
- That all possible defects are eliminated. This closes the enumerated
  correctness/delivery/startup/dashboard items with evidence; `remaining_blockers.md`
  lists what is still open.
