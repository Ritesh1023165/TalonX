1. **Verdict and SHAs**: Part 1 `TASK120_ACCOUNTING_CORRECTIONS_COMPLETE`; Part 2/3 `HARNESS_REUSE_CONFIRMED_WITH_OPERATIONAL_GAP_FINDING`; Part 5/6 `REPLAY_COMPLETE_ZERO_TRADE_RESULT`; Part 7 **`INSUFFICIENT_EVIDENCE_WITH_SPECIFIC_BLOCKER`**. Release verified `f28986999eec5e313cfc89db24e4dbacfb378891` unchanged (research-only, no release-branch work). Research `47530ce` → this commit, pushed.

2. **Task 120 accounting corrections**: withdrew "properly powered" wording (no formal power calculation was ever performed for N=57/19-issuer or N=10). Found and fixed a real bug: B3's `equity_final.equity` ($296,307.36) is **gross** of the 20bps research cost convention — V2's own paper ledger models zero cost internally, and the cost adjustment never flows back into it. Exact reconciliation: starting $300,000.00 + gross P&L −$3,692.63 − explicit cost adjustment −$1,140.00 (verified exactly = 57 trades × $10,000 avg notional × 20bps round-trip) = **cost-adjusted ending equity $295,167.37**. Cost applied exactly once (4 new tests confirm). The CI-includes-zero headline decision is unchanged.

3. **Experimental contract and harness-equivalence**: `EXPERIMENTAL_RELAXED_V1` = the SAME production `QuantScanner`/`evaluate_signals()` Original uses, with only `min_atr_pct`(0.25→0.10)/`confluence_score_min`(2→1)/`min_risk_reward_ratio`(1.5→1.0) relaxed — verified from source, locked fields (`volatility_gate_mode`, `confluence_contract`) confirmed unchanged. Reused an **existing**, already-tested production backtest engine (`talonx_backtest.engine.BacktestEngine`) — no new research platform built; 102 pre-existing tests (lookahead-safety/long-only-lifecycle/live-parity/state/reproducibility) cited as equivalence evidence, all pass. **Material finding**: `ExperimentalPaperEngine.check_exits()`/`.flatten_all()` have **no caller anywhere in the live runtime** — Experimental currently has no automatic exit mechanism in normal operation (the 9/11 "recovery-affected" exits were a manual intervention, consistent with this). Exit lifecycle used in this replay is labelled `DESIGNED_LIFECYCLE` (as-coded), not claimed `EXACT_CONTRACT`.

4. **Universe/window coverage**: all 35 configured symbols covered (0 missing), 0 data-quality blocking issues. Window: `2025-01-24 → 2025-01-31` (one calendar week, 116,295 bars) — narrowed from a full month (which was actually launched and observed at 5.0%/~8min, confirming a ~2.5hr completion) purely on elapsed-time grounds, before any Experimental outcome was inspected at either size. The rest of Segment A (~5.7 months) and all of Segment B (10 symbols, ~1yr) were not run — disclosed, not hidden.

5. **Trade frequency, economics, uncertainty**: 2,300 raw candidates, **75 fully gate-cleared published signals, 0 closed long trades**. The 75-published/75-`NO_ACTIVE_POSITION` exact match is consistent with every published candidate this week being BEARISH (a LONG_ONLY contract never opens on one) — an inference from the counts, not independently re-verified. Win rate/PF/net expectancy/CI are all `None` by construction (0 trades, 0 issuers) — not fabricated as zero-with-confidence. Ending equity: exactly $100,000.00 (no P&L of any kind this week).

6. **Execution/cost/concentration limitations**: 5bps round-trip spread modeled (matches `apply_spread`'s real formula exactly), commissions unmodeled, $2,500/trade allocation, $100,000 starting cash — all recorded but never exercised (0 trades). Stop/target would be evaluated against intrabar high/low (a disclosed, conservative execution-realism convention, not live's continuous-tick sampling) had any trade opened. Concentration/exposure/drawdown are all vacuous (0 trades).

7. **Decision**: **`INSUFFICIENT_EVIDENCE_WITH_SPECIFIC_BLOCKER`** — not negative economics (no P&L exists to be negative) and not an engine defect (the pipeline generated far more raw activity than Original ever does — 2,300 vs. Original's near-total silence). The exact blocker: one calendar week is too small a sample for this LONG_ONLY contract's realized direction mix to guarantee even one qualifying long entry.

8. **ONE next action**: re-run the SAME unmodified adapter (`research/scripts/task121_experimental_replay.py`) over a longer slice of the SAME already-available `task93_canonical_v1` dataset (extend `SEGMENT_A_END`; no new strategy, threshold, or adapter). Acceptance: ≥1 full month (a non-overlapping window from this one) producing either (a) ≥1 closed trade with the SAME predeclared issuer-block-bootstrap CI method, which is then genuinely evaluable, or (b) another zero-trade result, which would itself become informative about true entry frequency. Estimated cost: ~35-40 min/month at the measured rate, or ~15hrs for the full remaining Segment A; a per-symbol parallelization (engineering-only, no strategy change) would shorten this proportionally.

9. **Production/Redis preservation**: no release-branch change; Redis reachable (`ping()` True), 0 `talonx:*` keys; no application process left running (a stray duplicate replay process from an intentionally-abandoned one-month attempt was found alive under a different interpreter path — a Windows venv-shim child-process artifact — and explicitly killed by PID; confirmed only the intended run's process pair existed thereafter); no ports 8787/8760/8770/8501 listening.

10. **Journal/reports**: this entry (`request.md`/`record.md`/`outcome.md`); `docs/research/{TASK121_PROTOCOL,TASK121_EXPERIMENTAL_CONTRACT_RESULTS,TASK121_TASK120_ACCOUNTING_CORRECTIONS}.md`; `docs/research/evidence/task121/*` (3.1KB summary, small because 0 trades); 10 new focused tests, all passing; correction blockquote appended to `TASK120ABC_CORRECTED_BASELINE_AND_DECISION.md` — all at this commit, pushed to `research/talonx-profitability-2026-09`.

A well-supported negative result closes a question — this is not that; it is a well-supported *inconclusive* result with a specific, small, already-scoped next step. A passing test suite (102 pre-existing + 10 new, all green) does not establish a trading edge, and none is claimed here.
