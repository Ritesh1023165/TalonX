1. **Verdict and SHAs**: Part 1 `STALE_WORKTREE_ROOT_CAUSE_CONFIRMED`; Part 2/3 `CORRECTED_CONTRACT_PARITY_PROVEN`; Part 5/6 `REPLAY_COMPLETE_WITH_DISCLOSED_TELEMETRY_GAP`; Part 7 **`INSUFFICIENT_EVIDENCE_WITH_SPECIFIC_BLOCKER`**. Release verified `f28986999eec5e313cfc89db24e4dbacfb378891` — unchanged, research-only. Research `2f28922` → **`<this commit>`**, pushed.

2. **Why Task 121 reported the wrong exit behavior**: confirmed by git ancestry, not memory — the research branch forked from the release lineage at commit `9bec279` (Task 114); the release branch gained 38 subsequent commits, including `72baca2` (Task 118A P1, which wired `check_exits()` into `talonx_signals/run.py`'s live loop), never synced into research. Byte-hash comparison shows `run.py` is the ONLY module Task 121's adapter's dependency set touches that actually differs — every module the adapter *imports* (`talonx_quant/*`, `talonx_paper/engine.py`, `talonx_backtest/*`, `talonx_signals/{config,relaxed_profile,experimental_paper}.py`) is release-identical, so Task 121's computed ECONOMICS were unaffected; only its separate narrative read of `run.py` was wrong. "Friday's exits were manual" is withdrawn as unsupported, not replaced with a confirmed opposite claim.

3. **Correct contract**: entry fills at the signal's own bar CLOSE (immediate, not next-bar-open); exit is `check_exits()` (stop/target, single sampled price) ONLY — wired into every live tick (Task 118A P1); bearish signals do NOT close a position (nothing in `run.py` does this); **EOD flatten is NOT scheduled** (`flatten_all()` has no caller, confirmed directly) — positions can and do hold overnight/multi-day. Cost: `apply_spread(price, 5.0, side)` verified by a worked test to be ~5bps ROUND-TRIP (2.5bps/side), not 5bps per side — confirms, doesn't change, the prior label.

4. **Parity evidence**: `ExperimentalLifecycleShim` composes the REAL `ExperimentalPaperEngine.open_long`/`.check_exits` in place of `talonx_backtest`'s own (Original-shaped) trade simulator — not a reimplementation. 12 new tests (`tests/test_task121a_parity_trace.py`) cover all 9 required scenarios plus the spread worked-example and a zero-external-sends check; all pass. Provenance is verified programmatically before every replay (`verify_provenance()` — release worktree first on `sys.path`, every import's resolved file asserted under it, hashed, recorded in `module_manifest.json`).

5. **Replay window/scope/runtime**: one frozen calendar month, 2025-01-24→2025-02-23 inclusive, all 35 symbols, 375,628 bars, 0/35 missing. Backtest itself completed in 5,312.8s (~88.5 min), timed in-process. The post-backtest summary step then hung (>1hr, 100% CPU, a genuine reliability bug — likely unbounded log growth, fixed for future runs). Rather than discard 88.5 minutes of completed, durably-persisted ledger computation, the stuck process was killed and the SQLite ledger salvaged directly (read-only) — every number below is real, not estimated.

6. **Signal directions / entry-exit reconciliation**: funnel-level counts (raw candidates, publish/rejection breakdown, fine-grained exit reason) were lost with the stuck process before reaching disk — reported **UNAVAILABLE for this run**, not fabricated or backfilled. What IS real and complete: 33 closed round trips with full entry/exit prices, timestamps, holding duration, and realized P&L, read directly from the ledger.

7. **Economics**: N=33 closed trades, 18 distinct issuers, win rate 21.2%, PF 1.13, net $61.92 total (+$1.88/trade), ending equity $100,061.92 (0 open positions, genuinely resolved). Holding duration 3 min–~7.0 days (mean ~20.7h) — itself confirms the no-EOD-flatten correction is real. 95% issuer-block bootstrap CI on net $/trade: **[−$12.36, +$16.88] — includes zero.** Drop-top-1-issuer sensitivity doesn't flip the sign. Limitations: funnel telemetry unavailable this run; N=33/18 issuers is small.

8. **Decision**: **`INSUFFICIENT_EVIDENCE_WITH_SPECIFIC_BLOCKER`** — not reject (positive expectancy, PF>1, sensitivity-stable) and not advance (CI includes zero). Frequency (33/month, ~220× Original) is no longer the binding constraint — a materially more informative result than Task 121's zero-trade week.

9. **ONE next action**: extend the SAME frozen adapter/contract to the rest of Segment A (~5.7 more months) — no new methodology, no new threshold. Acceptance: a CI that excludes zero (either direction) at the accumulated trade count is decisive; continued CI-includes-zero at much higher N would itself be an informative (near-zero-edge) finding.

10. **Production/journal**: no release-branch change; Redis clean (0 `talonx:*` keys); no application process left running (verified after cleanup). Journal: `entries/2026-09-12_task121a_runtime_parity_repair/`; `docs/research/{TASK121A_PROVENANCE_AND_CONTRACT,TASK121A_CORRECTED_REPLAY_RESULTS}.md`; `docs/research/evidence/task121a/*` — all at this commit, pushed.

Priority followed as instructed: correct source → proven parity → completed replay → decision. The completed replay's own reliability gap is disclosed, not hidden behind a clean-looking number.
