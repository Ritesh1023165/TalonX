1. **Verdict**: real fix implemented, tested, and **deployed live today**. Release `0209ada`/`c88f4d4` → deployed **`5c0b3f3ccfef45ff8438e75f8f614b738deefc9a`** (merge of `4531fe2`), docs `0f5b455`. Research `49a054b` → this commit.

2. **Warmup functionality**: root-caused to `QuantScanner._preseed_1m_if_needed`'s `_preseeded_1m` marker being added **unconditionally on first attempt**, regardless of success/failure — meaning a symbol whose bulk-startup fetch failed could never get a second attempt for the rest of the process's life. Fixed via a new `run_bounded_recovery_sweep` (reuses the existing fetch path entirely; no new provider integration), re-verifying each not-ready symbol from the live buffer, clearing only that symbol's marker, and re-invoking the same `preseed_symbols()` call — bounded (2 attempts, 15s per-symbol timeout, 120s overall budget, backoff, one symbol at a time). Wired into the same pre-market-data safety window `run_initial_preseed` already uses, structurally excluding any race with live ticks.

3. **Tests**: 8 new tests, all passing, using a `RealisticFakeScanner` that models the real idempotency-guard semantics (bulk-failure recovery, partial recovery, no-duplicate-bars, structural live-tick-race exclusion via source inspection, no signal/Telegram output surface, no re-fetch of an already-recovered symbol, clean budget exhaustion, bounded provider-call volume). Broader `-k "preseed or warmup"` regression (106 tests) unaffected. **Real, isolated, bounded provider probe**: BLK/JNJ/ADC — all 3 fetched successfully just now (129/147/128 bars, <2s each), confirming the provider issue was transient.

4. **Readiness before/after**: **30/43 → 42/43 immediately post-restart** (live-verified against `quant.db.bar_buffer`, not the log alone). The recovery sweep's own log line was observed live: `Bounded preseed recovery sweep complete: 1/2 previously not-ready symbol(s) recovered in 2.6s (still incomplete: ['NUE'])`. **NUE remains the one honestly-reported not-ready symbol** (116/120 bars), expected to close via ordinary live accumulation within minutes — not silently treated as green.

5. **Deployment/restart**: deployed today. Full sequence: cutoff `15:50:06Z` → stop (`15:50` area, verified zero residual) → WAL-checkpointed backups of all 4 databases + `.env` → merge (`--no-ff`, clean, no conflicts) → restart `15:51:18Z` → **READY**, checkpoint daemon alive on the first attempt. **Continuity verified exactly**: all 9 Experimental `trade_history` rows and the one open SPCX position identical before/after; V2 cash/positions/ABCL-stale-episode-id unchanged; V2 fingerprint `11107198c5b81237` unchanged (preflight `[OK]`); no duplicate entries/exits/deliveries.

6. **Volatility-return result**: predeclared protocol (20-day pre-entry realized volatility, continuous, OLS slope, issuer-block + time-block bootstrap, A/B separate + pooled secondary, drop-MSTR sensitivity). **Within Population A**: slope −0.001733, CI excludes zero under **both** dependence assumptions (issuer-block [−0.00349,−0.000246], month-block [−0.00325,−0.000296]) — a real, method-robust negative association at the full-A sample. **Population B**: no relationship (CI includes zero both ways). **Drop-MSTR**: point estimate stays negative, but significance is lost (CI includes zero) — the full-A result is not robust to removing its largest contributor. **Decision: EXPLORATORY_ASSOCIATION_SUPPORTS_ONE_FURTHER_TEST** — confirmation requires genuinely new, future live 39-name-scope entries (same hypothesis as Task 118E, now with this report's stronger evidentiary basis); no re-slice of the already-used 2024–2026 history is treated as fresh.

7. **Experimental/V2 state, EOD**: SPCX still open, fresh mark $148.47 (42s old at check) → unrealized **+$4.09** (reversed from the earlier −$21.88 as price recovered). Realized P&L unchanged at −$324.4662160270568. V2: cash $300,000, 0 positions, unchanged. EOD not yet due (checked 15:54 UTC) — **`python -m talonx_ops.prospective close` required at/after 2026-09-11T20:00:00Z, complete by 21:30:00Z.**

8. **Remaining blockers**: NUE's exact readiness-completion time not observed to conclusion in this session (expected imminent); whether the volatility association survives genuinely new live data (the recommended further test, not yet run).

9. **Links**: `docs/audits/task118f_resilient_warmup/{IMPLEMENTATION,DEPLOYMENT}.md` (release, commits `0f5b455`/`5c0b3f3`); `docs/research/TASK118F_VOLATILITY_RETURN_TEST.md` (this branch).

**Operational readiness** (real fix, deployed, 42/43 live) and **trading profitability** (still not established for A; the volatility association is exploratory and MSTR-dependent) remain separate verdicts, as required.
