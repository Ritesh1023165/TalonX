1. **Diagnostic and actionable verdicts (separate)**: Diagnostic (Track A, daily association) = **`ASSOCIATION_SUPPORTED`** (95% CI on the incremental effect excludes zero, robust to both predefined sensitivities — qualified: the CI's lower bound does not fully clear the predeclared materiality band). Actionable candidate (Track B, pre-close mechanism) = **`NOT_SUPPORTED_UNDER_TESTED_CONTRACT`** (negative point estimate, CI includes zero, N=31 on the one frozen contract tested).

2. **SHAs**: release verified `f28986999eec5e313cfc89db24e4dbacfb378891` unchanged (research-only). Research `36b28ca` → **`<this commit>`**, pushed.

3. **Corrected timing contract**: Task 122's implicit claim that a same-day final-volume trigger could be acted on at that same day's close is withdrawn (final volume is only known at/after the close). Two explicit tracks defined: Track A — `S.close → next_XNYS_session.open`, non-actionable, a conditional historical-return study only; Track B — decision at a fixed **15:50 ET** pre-close cutoff using only same-time-of-day cumulative volume known by then, entry as a **reference fill** 2 minutes later (15:52 ET), exit at the next session's own open. No substitution of yesterday's volume or a post-close entry was made — both are different hypotheses.

4. **Coverage**: 43 active / 5 paused (ASML, PATH, PLTR, RIG, SMCI) of 48 configured. **38** active names have existing daily coverage (33+5 from two non-overlapping directories); **5** active names have none (BABA, BLSH, SHOP, SKHY, SPCX). **12** of the 38 also have existing 1-min intraday coverage. No single blended coverage fraction is claimed anywhere.

5. **Conditional/control returns, costs, uncertainty**: Track A — 2,557 triggers/67,608 eligible observations/1,905 distinct dates; trigger net mean **+0.1961%** vs. control (unconditional) net mean **+0.0274%** → incremental **+0.1687%/event**; date-block-bootstrap 95% CI on the incremental **[+0.0295%, +0.3085%]** (excludes zero); both sensitivities (drop-top-issuer MSTR, 2.5× trigger) agree in direction. Cost: 5bps round-trip, applied once. Materiality band ±10bps — only partially cleared (CI lower bound +2.95bps).

6. **Corporate-action/execution limitations**: Track A uses Alpaca `adjustment=all` (a total-return proxy, dividends embedded, not double-added); a known, already-documented partial-adjustment-artifact class is guarded against (|return|>50% excluded, 0 hits this run). Track B uses UNADJUSTED 1-min bars (no splits confirmed in-window by an existing audit, dividends not adjusted — a raw price return, NOT comparable to Track A's total-return figure). All Track B prices are explicitly labelled reference fills, never claimed executable.

7. **Actionable test result**: executed (data supported it) — 579 eligible, 31 triggers, incremental net **−0.5504%/event**, 95% CI **[−2.1445%, +1.2090%]** — includes zero, dominated by the small sample. Not read as contradicting Track A; read as: the tested contract shows no supported effect, and existing intraday coverage (12 symbols, ~7 months) cannot resolve it further.

8. **ONE next action**: close the overnight-attention hypothesis as an actionable candidate on currently available data — do not search alternative cutoffs/delays/windows (explicitly prohibited and would not fix the underlying coverage constraint). Exact missing input named: broader/longer 1-min intraday coverage for the 38 Track-A-eligible symbols via the SAME existing free access this program already uses — not pursued this task. Track A's association is retained as a disclosed research finding, not a promoted product signal.

9. **Production preservation**: no release-branch change; no application process started; Redis/production DBs re-verified clean.

10. **Journal/reports**: `entries/2026-09-13_task123_overnight_attention_diagnostic/` + `docs/research/{TASK123_TIMING_CORRECTION,TASK123_FROZEN_PROTOCOL,TASK123_OVERNIGHT_ATTENTION_RESULTS}.md` + `docs/research/evidence/task123/*` — all at this commit, pushed. Correction appended to `TASK122_CANDIDATE_DECISION.md` (original preserved).

Priority followed as instructed: causal contract → bounded computation → economic decision. A real association was found and reported honestly as non-actionable by construction; the one genuinely causal, executable version available from existing data did not clear its own bar, and that result is reported as a close, not reframed as a reason to keep searching.
