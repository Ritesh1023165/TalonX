============================= test session starts =============================
platform win32 -- Python 3.12.10, pytest-9.1.1, pluggy-1.6.0 -- C:\workspace\TalonX\.venv\Scripts\python.exe
cachedir: .pytest_cache
rootdir: C:\workspace\TalonX
configfile: pyproject.toml
plugins: anyio-4.14.2, langsmith-0.10.16, asyncio-1.4.0
asyncio: mode=Mode.STRICT, debug=False, asyncio_default_fixture_loop_scope=None, asyncio_default_test_loop_scope=function
collecting ... collected 14 items

tests/test_package1_settlement_integrity.py::test_sequential_duplicate_close_same_stale_snapshot FAILED [  7%]
tests/test_package1_settlement_integrity.py::test_concurrent_duplicate_close_two_independent_connections FAILED [ 14%]
tests/test_package1_settlement_integrity.py::test_duplicate_close_does_not_reset_cooldown_twice FAILED [ 21%]
tests/test_package1_settlement_integrity.py::test_close_position_returns_a_no_op_outcome_for_an_already_closed_position FAILED [ 28%]
tests/test_package1_settlement_integrity.py::test_close_position_failure_midway_rolls_back_completely PASSED [ 35%]
tests/test_package1_settlement_integrity.py::test_unresolved_position_retains_one_occupied_slot FAILED [ 42%]
tests/test_package1_settlement_integrity.py::test_unresolved_position_retains_its_invested_cost_on_the_row PASSED [ 50%]
tests/test_package1_settlement_integrity.py::test_mark_unresolved_credits_no_proceeds PASSED [ 57%]
tests/test_package1_settlement_integrity.py::test_unresolved_position_blocks_a_second_open_in_the_same_symbol FAILED [ 64%]
tests/test_package1_settlement_integrity.py::test_normal_open_still_permits_entry_after_a_genuine_close PASSED [ 71%]
tests/test_package1_settlement_integrity.py::test_unresolved_position_never_produces_complete_cash_only_equity FAILED [ 78%]
tests/test_package1_settlement_integrity.py::test_reconciliation_does_not_fabricate_a_cash_loss_for_unresolved FAILED [ 85%]
tests/test_package1_settlement_integrity.py::test_restart_preserves_unresolved_capacity_and_symbol_block FAILED [ 92%]
tests/test_package1_settlement_integrity.py::test_normal_open_and_closed_counts_unaffected_by_the_fix PASSED [100%]

================================== FAILURES ===================================
_____________ test_sequential_duplicate_close_same_stale_snapshot _____________

tmp_path = WindowsPath('C:/Users/rites/AppData/Local/Temp/pytest-of-rites/pytest-1799/test_sequential_duplicate_clos0')

    def test_sequential_duplicate_close_same_stale_snapshot(tmp_path):
        """Two calls to close_position() with the SAME stale in-memory
        position dict (as would happen if two overlapping callers each read
        the position while it was still OPEN) must credit cash and record a
        SELL exactly ONCE, not twice."""
        store, cfg, pos = _entered(tmp_path)
        cash_after_entry = store.cash()
    
        out1 = paper.close_position(store, pos, exit_price=105.0,
                                    exit_session=date(2026, 9, 22), config=cfg)
        out2 = paper.close_position(store, pos, exit_price=105.0,
                                    exit_session=date(2026, 9, 22), config=cfg)
    
        fresh = V2Store(str(tmp_path / "v.db"))
        proceeds = pos["shares"] * 105.0
>       assert fresh.cash() == pytest.approx(cash_after_entry + proceeds), (
            "cash was credited more than once for the same position close")
E       AssertionError: cash was credited more than once for the same position close
E       assert 311000.0 == 300500.0 ± 0.3005
E         
E         comparison failed
E         Obtained: 311000.0
E         Expected: 300500.0 ± 0.3005

tests\test_package1_settlement_integrity.py:79: AssertionError
_________ test_concurrent_duplicate_close_two_independent_connections _________

tmp_path = WindowsPath('C:/Users/rites/AppData/Local/Temp/pytest-of-rites/pytest-1799/test_concurrent_duplicate_clos0')

    def test_concurrent_duplicate_close_two_independent_connections(tmp_path):
        """Two INDEPENDENT V2Store connections (as two separate processes/
        threads would have), each holding its own stale snapshot of the same
        OPEN position, both call close_position() at the same time. SQLite's
        write lock serializes the two transactions, but that alone does not
        protect against duplicate economic effects unless the second call's
        own state-transition eligibility is actually checked."""
        store, cfg, pos = _entered(tmp_path)
        cash_after_entry = store.cash()
        path = str(tmp_path / "v.db")
    
        store_a = V2Store(path)
        store_b = V2Store(path)
        pos_a = store_a.all_positions()[0]
        pos_b = store_b.all_positions()[0]
    
        errors: list[BaseException] = []
    
        def _close(s, p):
            try:
                paper.close_position(s, p, exit_price=105.0,
                                     exit_session=date(2026, 9, 22), config=cfg)
            except BaseException as exc:  # noqa: BLE001
                errors.append(exc)
    
        t1 = threading.Thread(target=_close, args=(store_a, pos_a))
        t2 = threading.Thread(target=_close, args=(store_b, pos_b))
        t1.start()
        t2.start()
        t1.join(timeout=30)
        t2.join(timeout=30)
    
        assert not errors, f"unexpected exception(s) from concurrent close: {errors}"
    
        fresh = V2Store(path)
        proceeds = pos["shares"] * 105.0
>       assert fresh.cash() == pytest.approx(cash_after_entry + proceeds), (
            "concurrent duplicate close credited cash more than once")
E       AssertionError: concurrent duplicate close credited cash more than once
E       assert 311000.0 == 300500.0 ± 0.3005
E         
E         comparison failed
E         Obtained: 311000.0
E         Expected: 300500.0 ± 0.3005

tests\test_package1_settlement_integrity.py:122: AssertionError
_____________ test_duplicate_close_does_not_reset_cooldown_twice ______________

tmp_path = WindowsPath('C:/Users/rites/AppData/Local/Temp/pytest-of-rites/pytest-1799/test_duplicate_close_does_not_0')

    def test_duplicate_close_does_not_reset_cooldown_twice(tmp_path):
        store, cfg, pos = _entered(tmp_path)
        paper.close_position(store, pos, exit_price=105.0, exit_session=date(2026, 9, 22), config=cfg)
        cooldown_after_first = V2Store(str(tmp_path / "v.db")).cooldown_until("AAA")
    
        # a later, stale-snapshot duplicate close attempt with a DIFFERENT
        # (later) exit_session must not silently move the cooldown forward --
        # the second call is not a genuine settlement at all.
        paper.close_position(store, pos, exit_price=105.0, exit_session=date(2026, 9, 24), config=cfg)
        fresh = V2Store(str(tmp_path / "v.db"))
>       assert fresh.cooldown_until("AAA") == cooldown_after_first, (
            "a duplicate close call moved the re-entry cooldown forward")
E       AssertionError: a duplicate close call moved the re-entry cooldown forward
E       assert datetime.date(2026, 10, 1) == datetime.date(2026, 9, 29)
E        +  where datetime.date(2026, 10, 1) = cooldown_until('AAA')
E        +    where cooldown_until = <talonx_v2.store.V2Store object at 0x000001F9FA58D430>.cooldown_until

tests\test_package1_settlement_integrity.py:139: AssertionError
_ test_close_position_returns_a_no_op_outcome_for_an_already_closed_position __

tmp_path = WindowsPath('C:/Users/rites/AppData/Local/Temp/pytest-of-rites/pytest-1799/test_close_position_returns_a_0')

    def test_close_position_returns_a_no_op_outcome_for_an_already_closed_position(tmp_path):
        """The caller must be able to distinguish "I actually settled this"
        from "this was already settled" so it never generates a second exit
        notification for the same economic event."""
        store, cfg, pos = _entered(tmp_path)
        out1 = paper.close_position(store, pos, exit_price=105.0,
                                    exit_session=date(2026, 9, 22), config=cfg)
        out2 = paper.close_position(store, pos, exit_price=105.0,
                                    exit_session=date(2026, 9, 22), config=cfg)
>       assert out1.settled is True
               ^^^^^^^^^^^^
E       AttributeError: 'ExitOutcome' object has no attribute 'settled'

tests\test_package1_settlement_integrity.py:152: AttributeError
_____________ test_unresolved_position_retains_one_occupied_slot ______________

tmp_path = WindowsPath('C:/Users/rites/AppData/Local/Temp/pytest-of-rites/pytest-1799/test_unresolved_position_retai0')

    def test_unresolved_position_retains_one_occupied_slot(tmp_path):
        store, cfg, pos = _entered(tmp_path)
        store.mark_exit_unresolved(pos["position_id"], detail="no bar found")
>       assert store.n_open() == 1, (
            "an EXIT_UNRESOLVED position must still occupy one capacity slot")
E       AssertionError: an EXIT_UNRESOLVED position must still occupy one capacity slot
E       assert 0 == 1
E        +  where 0 = n_open()
E        +    where n_open = <talonx_v2.store.V2Store object at 0x000001F9FA595190>.n_open

tests\test_package1_settlement_integrity.py:185: AssertionError
______ test_unresolved_position_blocks_a_second_open_in_the_same_symbol _______

tmp_path = WindowsPath('C:/Users/rites/AppData/Local/Temp/pytest-of-rites/pytest-1799/test_unresolved_position_block0')

    def test_unresolved_position_blocks_a_second_open_in_the_same_symbol(tmp_path):
        store, cfg, pos = _entered(tmp_path, symbol="AAA", episode_id="ep1")
        store.mark_exit_unresolved(pos["position_id"], detail="no bar found")
    
        outcome = paper.enter_position(store, _decision(episode_id="ep2", symbol="AAA"),
                                       entry_price=50.0, entry_session=date(2026, 9, 9), config=cfg)
>       assert outcome.entered is False, (
            "an OPEN-only symbol lookup silently permitted a second position "
            "in a symbol that already has an unresolved obligation"
        )
E       AssertionError: an OPEN-only symbol lookup silently permitted a second position in a symbol that already has an unresolved obligation
E       assert True is False
E        +  where True = EntryOutcome(entered=True, reason='ENTERED', position_id=2, shares=200.0, target_exit_session=datetime.date(2026, 9, 23)).entered

tests\test_package1_settlement_integrity.py:211: AssertionError
______ test_unresolved_position_never_produces_complete_cash_only_equity ______

tmp_path = WindowsPath('C:/Users/rites/AppData/Local/Temp/pytest-of-rites/pytest-1799/test_unresolved_position_never0')

    def test_unresolved_position_never_produces_complete_cash_only_equity(tmp_path):
        from talonx_ops import paper_performance
    
        store, cfg, pos = _entered(tmp_path)
        store.mark_exit_unresolved(pos["position_id"], detail="no bar found")
    
        snap = paper_performance._v2_snapshot(
            Path(store.path), now=datetime(2026, 9, 22, tzinfo=timezone.utc),
            session_date="2026-09-22", price_source_db=None)
    
>       assert snap["equity"]["status"] != "COMPLETE", (
            "equity was reported COMPLETE while an EXIT_UNRESOLVED position's "
            "value is entirely unknown"
        )
E       AssertionError: equity was reported COMPLETE while an EXIT_UNRESOLVED position's value is entirely unknown
E       assert 'COMPLETE' != 'COMPLETE'

tests\test_package1_settlement_integrity.py:239: AssertionError
______ test_reconciliation_does_not_fabricate_a_cash_loss_for_unresolved ______

tmp_path = WindowsPath('C:/Users/rites/AppData/Local/Temp/pytest-of-rites/pytest-1799/test_reconciliation_does_not_f0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x000001F9FA596C00>

    def test_reconciliation_does_not_fabricate_a_cash_loss_for_unresolved(tmp_path, monkeypatch):
        import talonx_ops.prospective.close as close_mod
    
        store, cfg, pos = _entered(tmp_path, cash=300_000.0)
        store.mark_exit_unresolved(pos["position_id"], detail="no bar found")
    
        monkeypatch.setattr(close_mod, "V2_DB_PATH", str(tmp_path / "v.db"))
        monkeypatch.setattr(close_mod, "CAMPAIGN_STARTING_CASH", 300_000.0)
    
        rec, asserts, findings = close_mod._v2_reconcile()
>       assert asserts["cash_plus_open_cost_reconciles"] == "PASS", (
            f"reconciliation fabricated a mismatch purely because the "
            f"EXIT_UNRESOLVED position's cost was omitted: {findings}"
        )
E       AssertionError: reconciliation fabricated a mismatch purely because the EXIT_UNRESOLVED position's cost was omitted: ['cash_plus_open_cost_reconciles: cash 290000.0 + open_cost 0.0 != 300000.0 + realized 0.0']
E       assert 'FAIL' == 'PASS'
E         
E         - PASS
E         + FAIL

tests\test_package1_settlement_integrity.py:255: AssertionError
_________ test_restart_preserves_unresolved_capacity_and_symbol_block _________

tmp_path = WindowsPath('C:/Users/rites/AppData/Local/Temp/pytest-of-rites/pytest-1799/test_restart_preserves_unresol0')

    def test_restart_preserves_unresolved_capacity_and_symbol_block(tmp_path):
        store, cfg, pos = _entered(tmp_path)
        store.mark_exit_unresolved(pos["position_id"], detail="no bar found")
    
        # simulate a restart: a brand-new V2Store instance against the same file
        restarted = V2Store(str(tmp_path / "v.db"))
>       assert restarted.n_open() == 1
E       assert 0 == 1
E        +  where 0 = n_open()
E        +    where n_open = <talonx_v2.store.V2Store object at 0x000001F9FA587A10>.n_open

tests\test_package1_settlement_integrity.py:267: AssertionError
=========================== short test summary info ===========================
FAILED tests/test_package1_settlement_integrity.py::test_sequential_duplicate_close_same_stale_snapshot
FAILED tests/test_package1_settlement_integrity.py::test_concurrent_duplicate_close_two_independent_connections
FAILED tests/test_package1_settlement_integrity.py::test_duplicate_close_does_not_reset_cooldown_twice
FAILED tests/test_package1_settlement_integrity.py::test_close_position_returns_a_no_op_outcome_for_an_already_closed_position
FAILED tests/test_package1_settlement_integrity.py::test_unresolved_position_retains_one_occupied_slot
FAILED tests/test_package1_settlement_integrity.py::test_unresolved_position_blocks_a_second_open_in_the_same_symbol
FAILED tests/test_package1_settlement_integrity.py::test_unresolved_position_never_produces_complete_cash_only_equity
FAILED tests/test_package1_settlement_integrity.py::test_reconciliation_does_not_fabricate_a_cash_loss_for_unresolved
FAILED tests/test_package1_settlement_integrity.py::test_restart_preserves_unresolved_capacity_and_symbol_block
========================= 9 failed, 5 passed in 1.46s =========================
