# Remaining bounded follow-ups (not expanded here)
- SHUTDOWN notice not delivered after `close` (needs in-process drain before stop); operator checkpoint `campaign` block still shows legacy constants; `close` writes `v2_lane.db.eod-copy` name; `/ping` "broad-discovery-inclusive" wording.
- Start the stack >= 45 min before pre-open so the insider store is fresh (Intelligence catch-up ~30 min in the canary).
- Old bot tokens remain valid at Telegram until revoked (user action). Git history not exhaustively scanned for tokens (HEAD and ignored dirs clean).
- OPS-017 stale literals; IDENTITY_MISMATCH detector; five-state readiness / watchdog; provider correction metadata; rate-limit stress.
