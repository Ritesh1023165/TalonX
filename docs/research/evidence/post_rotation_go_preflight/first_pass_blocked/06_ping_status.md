# /ping
**PING: USER_ACTION_REQUIRED.** I cannot send a Telegram command as the operator. Structural path unchanged and test-covered (`/ping` V2 section from the live status file; primary listener is the owner). Because `TELEGRAM_BOT_TOKEN`
(the primary listener bot) is not yet rotated, /ping must be confirmed AFTER that token is rotated.
